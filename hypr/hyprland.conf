#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
#monitor=eDP-1, 1366x768, 0x0, 1
monitor = , preferred, auto, 1

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
$terminal = alacritty
$browser = librewolf
$store = veracrypt
$wallet = keypassxc

exec-once = sh $HOME/.config/hypr/scripts/nm-applet-no-systray.sh
exec-once = waybar
exec-once = hyprpaper
exec-once = sh ~/.config/hypr/scripts/random-wallpaper.sh &
exec-once = wl-paste --watch cliphist store 
exec-once = dunst -conf .config/dunst/dunstrc
exec-once = easyeffects --gapplication-service
exec-once = hypridle
exec-once = sh ~/.config/hypr/scripts/nogaps.sh
exec-once = swayidle -w timeout 300 'swaylock -f' timeout 600 'sleep' before-sleep 'swaylock -f' 

# startup apps
exec-once = [ workspace 1 silent ] $terminal
exec-once = [ workspace 2 silent ] $browser
exec-once = [workspace 3 silent] $store
exec-once = [workspace 3 silent] $wallet


# Source a file (multi-file configs)
# source = ~/.config/hypr/bind.conf
# source =~/.config/hypr/hyprpaper.conf

# Some default env vars.
env = XCURSOR_SIZE,24
env = XDG_SESSION_TYPE,wayland
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_DESKTOP,Hyprland

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/

input {
    kb_layout = br
    kb_variant = abnt2
    kb_model = pc105
    kb_options =
    kb_rules = 

    # numlock_by_default = true

    follow_mouse = 1
    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

# Corne keyboard - US International (dead keys for accents)
device {
    name = ifkb-corne
    kb_layout = us
    kb_variant = intl
    kb_model =
    kb_options =
}

device {
    name = ifkb-corne-keyboard
    kb_layout = us
    kb_variant = intl
    kb_model =
    kb_options =
}

# keybinding

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod_SHIFT, RETURN, exec, $terminal --class floating
bind = $mainMod, Q, killactive,  
bind = ALT, F4, killactive,
bind = $mainMod, F11, fullscreen 
bind = $mainMod, E, exec, dolphin
bind = $mainMod, F, exec, $browser 
bind = $mainMod_SHIFT, F, exec, $browser --private-window
bind = $mainMod, T, exec, Telegram
bind = $mainMod, L, exec, hyprlock
bind = $mainMod, S, togglefloating, 
bind = $mainMod_SHIFT, R, exec, ~/.config/hypr/scripts/reload.sh # reload hyprland 
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
# bind = $mainMod, K, exec, killall waybar || waybar # hide or kill and display waybar

# keybindings helper overlay
bind = $mainMod_SHIFT, slash, exec, alacritty --class hypr-helper -o 'window.opacity=0.15' -o 'window.decorations="None"' -o 'window.padding.x=32' -o 'window.padding.y=24' -o 'colors.primary.background="#1e1e2e"' -o 'colors.primary.foreground="#f9e2af"' -e ~/.config/hypr/scripts/keybinds-helper.sh

# rofi
# bindr = $mainMod, Super_L, exec, ~/.config/rofi/launchers/type-2/launcher.sh  # launch the app list in rofi with super key only
bind = $mainMod, SPACE, exec, ~/.config/rofi/launchers/launcher.sh
bind = $mainMod, X, exec, ~/.config/rofi/powermenu/type-2/powermenu.sh
# bind = $mainMod, R, exec, wofi --show drun
# bind = $mainMod, R, exec, ~/.config/rofi/run/run.sh
# bind = $mainMod, Z, exec, ~/.config/rofi/filebrowser/filebrowser.sh
# bind = $mainMod, V, exec, ~/.config/rofi/clipboard/clipboard.sh
# bind = $mainMod_SHIFT, V, exec, ~/.config/rofi/snippet/snippet.sh
bind = $mainMod_SHIFT, SPACE, exec, ~/.config/rofi/emoji/emoji.sh
# bind = $mainMod, W, exec, ~/.config/rofi/wifi/wifi.sh
# bind = $mainMod_SHIFT, W, exec, ~/.config/rofi/wifi/wifinew.sh
# bind = $mainMod, B, exec, ~/.config/rofi/bluetooth/blt-connect.sh

# Screenshot
bind = SHIFT, Print, exec, FILE=~/Pictures/Screenshots/Screenshot-$(date +%F_%T).png; grim -g "$(slurp)" - | tee >(wl-copy) | { cat > "$FILE" && [ -s "$FILE" ] && dunstify -i "$FILE" "Screenshot of the region taken" -t 1000 || rm -f "$FILE"; }
bind = , Print, exec, grim - | wl-copy && wl-paste > ~/Pictures/Screenshots/Screenshot-$(date +%F_%T).png | dunstify  -i ~/Pictures/Screenshots/Screenshot-$(date +%F_%T).png "Screenshot of whole screen taken" -t 1000 # screenshot of the whole screen

# Poweroff, Logout, Reboot
bind = $mainMod_SHIFT_ALT, P, exec, shutdown -h now
bind = $mainMod_SHIFT_ALT, R, exec, reboot
bind = $mainMod_SHIFT_ALT, L, exit,
bind = $mainMod_SHIFT_ALT, S, exec, sleep 1 && hyprctl dispatch dpms off	# turn off screen 
bind = $mainMod_SHIFT, S, exec, sleep 1 && hyprctl dispatch dpms on		# turn on screen

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
bind = ALT, Tab, cyclenext
bind = $mainMod, Tab, swapnext

# Switch workspaces with mainMod + [1-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9

# Move active window to a workspace with mainMod + SHIFT + [1-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
bind = $mainMod SHIFT, right, resizeactive, 10 0
bind = $mainMod SHIFT, left, resizeactive, -10 0
bind = $mainMod SHIFT, up, resizeactive, 0 -10
bind = $mainMod SHIFT, down, resizeactive, 0 10

# brightness control
bind=,XF86MonBrightnessDown,exec,brillo -q -U 5 && dunstify -h int:value:"$(( ($(cat /sys/class/backlight/*/brightness) * 100) / $(cat /sys/class/backlight/*/max_brightness) ))"  -i ~/.config/dunst/assets/brightness.svg -t 500 -r 2593 "Brightness: $(( ($(cat /sys/class/backlight/*/brightness) * 100) / $(cat /sys/class/backlight/*/max_brightness) ))%"
bind=,XF86MonBrightnessUp,exec,brillo -q -A 5 && dunstify -h int:value:"$(( ($(cat /sys/class/backlight/*/brightness) * 100) / $(cat /sys/class/backlight/*/max_brightness) ))"  -i ~/.config/dunst/assets/brightness.svg -t 500 -r 2593 "Brightness: $(( ($(cat /sys/class/backlight/*/brightness) * 100) / $(cat /sys/class/backlight/*/max_brightness) ))%"

# audio/sound control
bind = , xf86audioraisevolume, exec, pamixer -i 5 && dunstify -h int:value:"$(pamixer --get-volume)" -i ~/.config/dunst/assets/volume.svg -t 500 -r 2593 "Volume: $(pamixer --get-volume) %" 
bind = , xf86audiolowervolume, exec, pamixer -d 5 && dunstify -h int:value:"$(pamixer --get-volume)" -i ~/.config/dunst/assets/volume.svg -t 500 -r 2593 "Volume: $(pamixer --get-volume) %" 
bind = , xf86AudioMute, exec, pamixer -t && dunstify -i ~/.config/dunst/assets/$(pamixer --get-mute | grep -q "true" && echo "volume-mute.svg" || echo "volume.svg") -t 500 -r 2593 "Toggle Mute"
bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioNext, exec, playerctl next
bind = , XF86AudioPrev, exec, playerctl previous
bind = , XF86audiostop, exec, playerctl stop

# clipboard
binde = $mainMod, C, sendshortcut, CTRL, INSERT
binde = $mainMod, V, sendshortcut, SHIFT, INSERT
bind = $mainMod_SHIFT, V, exec, ~/.config/rofi/clipboard/clipboard.sh

misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 10
    border_size = 2
    col.active_border = 0xffcba6f7
    col.inactive_border = 0xff313244
    # no_border_on_floating = true
    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 5
    active_opacity = 1.0
    inactive_opacity = 1.0
    blur {
        enabled = yes
        size = 10
        passes = 1
        new_optimizations = on
    }
    shadow {
        enabled= true
        ignore_window = true
        range = 4
        offset = 2 2
        render_power = 2
        color = 0x66000000
    }
}

animations {
    enabled = true

    # Some default animations, see https://wiki.hyprland.org/Configuring>

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = true # master switch for pseudotiling. Enabling is boun>
    preserve_split = true # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_status = master
    new_on_top = true
}

# gestures {
#    # See https://wiki.hyprland.org/Configuring/Variables/ for more
#    workspace_swipe = true
# }

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
# device:epic-mouse-v1 {
#     sensitivity = -0.5
# }

# Example windowrule v1
# windowrule = float, ^(kitty)$

# dracula/hyprland
general {
    #col.active_border = rgb(44475a) rgb(bd93f9) 90deg
    #col.inactive_border = rgba(44475aaa)
    #col.group_border = rgba(282a36dd)
    #col.group_border_active = rgb(bd93f9) rgb(44475a) 90deg
    # non-gradient alternative
    #col.active_border = rgb(bd93f9)
    #col.inactive_border = rgba(44475aaa)
    #col.group_border = rgba(282a36dd)
    #col.group_border_active = rgb(bd93f9)
    col.active_border = rgb(268bd2) # rgb(6272a4) # or rgb(44475a)
    col.inactive_border = rgb(0267ae)
   # col.nogroup_border = rgb(282a36)
   # col.nogroup_border_active = rgb(6272a4) # or rgb(44475a)

}

# Window rules
# Never go to fullscrenn when book
windowrule = match:float true, fullscreen_state 0 0
windowrule = match:float true, maximize off
windowrule = match:float true, fullscreen off

# Block client-driven maximize/fullscreen events for floating windows
windowrule = match:float true, suppress_event maximize
windowrule = match:float true, suppress_event fullscreen

# Default float geometry
windowrule = match:float true, size 60% 70%
windowrule = match:float true, center on

# Cursor → fullscreen + opaque
windowrule = match:class ^(Cursor)$, fullscreen on
windowrule = match:class ^(Cursor)$, opaque on

# Center all floating windows
windowrule = match:float 1, center 1

########################
# FLOATING DIALOG RULES
########################
# Utilities
windowrule = match:class ^(org.pulseaudio.pavucontrol)$, float on
windowrule = match:class ^(org.pulseaudio.pavucontrol)$, center on
windowrule = match:class ^(blueman-manager)$, float on
windowrule = match:class ^(nm-connection-editor)$, float on
windowrule = match:class ^(vial)$, float on
# Common dialog-ish classes — normalize
windowrule = match:class ^(dialog|confirm|error|notification|splash|confirmreset)$, size 900 600
windowrule = match:class ^(dialog|confirm|error|notification|splash|confirmreset)$, center on
windowrule = match:class ^(dialog|confirm|error|notification|splash|confirmreset)$, float on

# File pickers / dialogs by title
windowrule = match:title ^(Open File)(.)$, float on
windowrule = match:title ^(Select a File)(.)$, float on
windowrule = match:title ^(Choose wallpaper)(.)$, float on
windowrule = match:title ^(Open Folder)(.)$, float on

####################################
# SMALL FLOATING TERMINAL
####################################
windowrule = match:class ^(floating)$, float on
windowrule = match:class ^(floating)$, size 40% 40%
windowrule = match:class ^(floating)$, center on

####################################
# KEYBINDINGS HELPER OVERLAY
####################################
windowrule = match:class ^(hypr-helper)$, float on
windowrule = match:class ^(hypr-helper)$, size 65% 75%
windowrule = match:class ^(hypr-helper)$, center on

# Fix XWayland dragging issues / ghost windows
windowrule = match:xwayland true, match:class ^$, match:title ^$, no_focus on

####################################
# STEAM GAMES
####################################
windowrule = match:class ^(steam_app_.*)$, float off
windowrule = match:class ^(steam_app_.*)$, fullscreen on
